go get -u github.com/swaggo/swag/cmd/swag@v1.6.5
go get -u github.com/swaggo/gin-swagger@v1.2.0
go get -u github.com/swaggo/files
go get -u github.com/alecthomas/template
swag -v 
swag init
http://127.0.0.1:8000/swagger/index.html#

curl -X PUT 'http://127.0.0.1:8000/api/v1/tags/1' -F state=0 -F modified_by=tom

docker run -d --name jaeger -e COLLECTOR_ZIPKIN_HTTP_PORT=9411 -p 5775:5775/udp -p 6831:6831/udp -p 6832:6832/udp -p 5778:5778 -p 16686:16686 -p 14268:14268 -p 9411:9411 jaegertracing/all-in-one:1.16

http://localhost:16686

使用集中式配置中心,viper+etcd

go run -x -n
go build -o 
参数
-x 打印编译的参数,生成二进制
-n 打印编译过程,不生成二进制
-a 强制重新编译所有涉及依赖
-o 生成二进制文件
-p 指定编译过程中可以并发运行程序的数量，默认为可用cpu数量
-work 打印临时文件目录
-race 启用数据竞争检测
-import 从文件中读如配置
-s 省略符号表并调试信息
-w 省略DWARF符号表
-buildmode mode 设置构建模式
-build id 将id记录为go工具链的构建id
-extld linker 设置外部链接器（默认　gcc）

GOCACHE 编译缓存

go clean -cache

设置临时目录
GOTMPDIR

缩小二进制文件
-w 
-s 

go build -ldflags "-s -w" -o blog

编译生成版本号和提交信息
go build -ldflags "-X main.buildTime=`date +%Y-%m-%d,%H:%M:%S` -X main.buildVersion=1.0.0 -X main.gitCommitID=`git rev-parse HEAD`"
./blog -version

fsnotify 使用
air使用
go get -u github.com/cosmtrek/air
air -c .air.conf

wechat_user 信息
成功时候的response
response_code

每个接口添加接口执行时间

实现多个图拍上传
利用redis实现限流器
token bucket



